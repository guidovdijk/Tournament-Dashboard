const baseUrl = 'http://localhost:8090';

export const url = {
  user: baseUrl + '/user',
  players: baseUrl + '/players',
  teams: baseUrl + '/tournaments/teams',
  tournaments: baseUrl + '/tournaments',
}